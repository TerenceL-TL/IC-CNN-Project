from models import *

train_imgs = 

base = UNet(input_width = 250,
            input_height = 250,
            num_classes = 2,
            train_images = ,
            train_instances,
            val_images,
            val_instances,
            epochs,
            lr,
            lr_decay,
            batch_size,
            save_path
            )

